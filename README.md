MoneyMate – Chatbot Financeiro com LLaMA 3.3 (Groq) + Flask + Supabase

MoneyMate é um chatbot financeiro capaz de interpretar mensagens em linguagem natural, extrair valores de gastos e classificar automaticamente cada despesa usando o modelo LLaMA 3.3 70B via Groq API.

O sistema roda localmente usando Flask, possui uma interface web simples e armazena registros no Supabase.

🚀 Funcionalidades

Interpretação de texto natural (Ex: "Uber 25 reais", "Comprei um lanche por 22,90")

Extração de valor e descrição

Categorização automática de gastos usando IA (Groq)

Registro persistido no banco Supabase

Interface web local em HTML/CSS/JS

API HTTP servida via Flask para comunicação com o front-end
